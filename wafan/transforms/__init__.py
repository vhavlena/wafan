"""Per-transform SMT-LIB2 modelling modules.

Each ``t:`` SecRule transformation that requires non-trivial modelling
(beyond a direct SMT-LIB built-in or a simple uninterpreted function)
gets its own module here.
"""
